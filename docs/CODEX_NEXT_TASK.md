# 下一条 Codex 任务门禁

M0.2b.2“Concrete strategy identification 与 occupancy conflicts”完成后，先审查独立提交和
全部质量检查。

未经明确批准，不要继续 M0.2b.3、M0.2c、M0.2d 或路线 M1。

下一增量 M0.2b.3 只考虑：

- legacy snapshot evidence 的显式、幂等迁移；
- 旧 `StrategySelectionParticipant.strategy_id` 与新 identification record 的审慎边界；
- revision correction 后 legacy/catalog-derived interpretation 的失效行为；
- snapshot completeness 重新定位为 prebattle data quality。

不得提前实施 BattleRoster、runtime slots、assignment、
annotation、OCR、OpenCV、capture、桌面 UI、自动点击、真实 catalog 或路线 M1。
