# 下一条 Codex 任务门禁

M0.2b.1“Prebattle evidence 与 ready-confirmed commitment”完成后，先审查独立提交和
全部质量检查。

未经明确批准，不要继续 M0.2b.2、M0.2b.3、M0.2c、M0.2d 或路线 M1。

下一增量 M0.2b.2 只考虑：

- basis-aware concrete strategy identification；
- `CATALOG_DERIVED` 才强制携带 `RulesetDependencyStamp`；
- direct/manual claim 的当前 catalog compatibility；
- duplicate occupancy、participant identification 和 catalog compatibility 三类冲突；
- 显式 supersession 与可审计 manual correction；
- 无冲突、fresh、compatible identification 的 read model。

不得提前实施 legacy snapshot migration、BattleRoster、runtime slots、assignment、
annotation、OCR、OpenCV、capture、桌面 UI、自动点击、真实 catalog 或路线 M1。
