# 下一条 Codex 任务门禁

M0.2b.3“Legacy snapshot 迁移与 revision 失效兼容”已形成独立本地提交后，先等待用户
审查提交和全部质量检查结果。

未经明确批准，不要继续 M0.2c、M0.2d 或路线 M1。

M0.2b.3 已完成的边界：

- legacy snapshot evidence 的显式、幂等、可审计迁移；
- legacy ready 的 typed evidence 与 commitment 接入；
- legacy strategy interpretation 的弱记录与 dependency freshness；
- snapshot completeness 作为 prebattle data quality；
- frozen snapshot 与独立 evidence/history 流的分离；
- legacy materialized query 与新 occupancy authority query 的分离。

下一步若获批准进入 M0.2c，才考虑 BattleRoster、runtime slots、association 与
assignment。仍不得擅自实现 annotation、OCR、OpenCV、capture、桌面 UI、自动点击、
真实 catalog 或路线 M1。
