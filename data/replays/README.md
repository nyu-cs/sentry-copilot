# Replay annotations

Store annotation metadata here, but keep recordings under `local_data/replays/`.
Treat a complete match as one evaluation unit; never split adjacent frames from the same video across train and test sets.
